spring:
  application:
    name: auth-service

  # Everything else (DB, JWT secret, tracing endpoint) comes from config-repo via Config
  # Server. Non-optional on purpose: a missing Config Server is a loud startup failure,
  # not a silent fallback to stale local defaults.
  config:
    import: configserver:${CONFIG_SERVER_URL:http://localhost:8888}
