# Copy to .env on the data VM itself (never commit the real .env).
# Temporary plaintext passwords -- Phase 14 replaces these with Secret Manager.

MYSQL_ROOT_PASSWORD=changeme
ORDER_DB_PASSWORD=changeme
INVENTORY_DB_PASSWORD=changeme
