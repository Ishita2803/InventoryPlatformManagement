apiVersion: apps/v1
kind: Deployment
metadata:
  name: inventory-service
  namespace: default
spec:
  replicas: 1
  selector:
    matchLabels:
      app: inventory-service
  template:
    metadata:
      labels:
        app: inventory-service
    spec:
      serviceAccountName: order-platform-workload
      containers:
        - name: inventory-service
          image: us-central1-docker.pkg.dev/inventorymanagement-507107/order-platform-repo/inventory-service:204439c
          # The Secret Manager CSI driver's own service account lacks cluster-scope RBAC to
          # list/watch Secrets, so secretObjects (mirroring the mount into a native k8s
          # Secret for secretKeyRef) never succeeds -- a gap in the GKE-managed component,
          # not something fixable here. The volume mount itself works, so read the file
          # directly instead of depending on that sync.
          command: ["sh", "-c"]
          args:
            - export MYSQL_PASSWORD="$(cat /mnt/secrets-store/inventory/mysql-inventory-password)" && exec java $JAVA_OPTS org.springframework.boot.loader.launch.JarLauncher
          ports:
            - containerPort: 8082
          envFrom:
            - configMapRef:
                name: platform-config
          env:
            - name: MYSQL_USER
              value: "inventory_app"
          resources:
            requests:
              memory: "768Mi"
              cpu: "100m"
            limits:
              memory: "768Mi"
              cpu: "750m"
          readinessProbe:
            httpGet:
              path: /actuator/health
              port: 8082
            initialDelaySeconds: 30
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /actuator/health
              port: 8082
            initialDelaySeconds: 60
            periodSeconds: 15
          volumeMounts:
            - name: mysql-inventory-password
              mountPath: "/mnt/secrets-store/inventory"
              readOnly: true
      volumes:
        - name: mysql-inventory-password
          csi:
            driver: secrets-store-gke.csi.k8s.io
            readOnly: true
            volumeAttributes:
              secretProviderClass: mysql-inventory-password
---
apiVersion: v1
kind: Service
metadata:
  name: inventory-service
  namespace: default
spec:
  selector:
    app: inventory-service
  ports:
    - port: 8082
      targetPort: 8082
