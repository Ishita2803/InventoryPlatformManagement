apiVersion: apps/v1
kind: Deployment
metadata:
  name: vendor-service
  namespace: default
spec:
  replicas: 1
  selector:
    matchLabels:
      app: vendor-service
  template:
    metadata:
      labels:
        app: vendor-service
    spec:
      serviceAccountName: order-platform-workload
      containers:
        - name: vendor-service
          image: us-central1-docker.pkg.dev/inventorymanagement-507107/order-platform-repo/vendor-service:latest
          ports:
            - containerPort: 8086
          envFrom:
            - configMapRef:
                name: platform-config
          env:
            - name: MYSQL_USER
              value: "vendor_app"
            - name: MYSQL_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: impulse-secrets
                  key: vendor-db-password
          resources:
            requests:
              memory: "512Mi"
              cpu: "50m"
            limits:
              memory: "512Mi"
              cpu: "500m"
          readinessProbe:
            httpGet:
              path: /actuator/health
              port: 8086
            initialDelaySeconds: 30
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /actuator/health
              port: 8086
            initialDelaySeconds: 60
            periodSeconds: 15
---
apiVersion: v1
kind: Service
metadata:
  name: vendor-service
  namespace: default
spec:
  selector:
    app: vendor-service
  ports:
    - port: 8086
      targetPort: 8086
